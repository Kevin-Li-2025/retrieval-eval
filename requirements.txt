# Core (no local embeddings)
numpy>=1.24.0
pyyaml>=6.0
click>=8.0
tqdm>=4.65.0
openai>=1.0.0
qdrant-client>=1.7.0
httpx>=0.25.0

# Optional: pip install -e ".[local]"
# sentence-transformers>=2.2.0
# torch>=2.0.0
